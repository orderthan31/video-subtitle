[project]
name = "video-subtitle-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["video-service-shared", "google-genai==2.23.0", "httpx>=0.28.1,<1"]

[project.scripts]
video-media-worker = "media_worker.worker:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["media_worker*"]
